# FloatingClock 项目说明

## 定位

Windows 悬浮时钟，服务于限时活动的多平台时间源查看、毫秒显示和倒计时。

## 常用命令

```powershell
npm install
npm test
Set-Location src-tauri; cargo test; Set-Location ..
npm run dev
npm run tauri:build
```

正式打包唯一入口是 `npm run tauri:build`；版本号需同步 `package.json`、`package-lock.json` 根包、`src-tauri/Cargo.toml` 和 `src-tauri/tauri.conf.json`。

## 技术与目录

- Tauri 2 + Rust + WebView2；前端在 `src/`，Rust 宿主和打包配置在 `src-tauri/`。
- `test/` 保存 Node 回归测试；`docs/` 保存打包、校时、更新和功能调研。
- `dist/` 是分发文件，`src-tauri/target/` 是构建中间产物，均不提交。

## 约定

- 时间源只使用匿名、只读、可验证的公开端点；失败必须保留既有回退链，不接入 Cookie、账号态或业务下单接口。
- `.tauri` 下的签名私钥和 DPAPI 密码文件在仓库外保管，绝不写入代码、日志或 Git。
- 修改后先运行相关测试；提交、推送、发布和删除操作需要用户明确授权。

## UI 设计与验证

- 标准模式内容尺寸固定为 392×392；Mini 模式高度固定为 92，宽度由现有自动调整逻辑在 236–1200 范围内计算。UI 修改不得通过改变窗口尺寸解决布局问题，尺寸变更需单独确认。
- 同一需求存在多种视觉方案时，实施前先说明方案、影响和推荐意见；简单且无歧义的小调整可直接实施。
- 优先使用不改变布局尺寸的手段（颜色、边框、阴影、短动画）；增加行高、内边距或纵向结构前先核算固定窗口内的可用空间。
- UI 修改默认运行相关测试并进行静态结构和尺寸检查；只有用户主动要求时，才使用 Computer Use 或其他实际窗口、截图验收手段。
- 报告时区分本地代码/测试验证与用户视觉验收；完成前者不等于用户已经确认最终效果。

## 当前状态

- `v0.1.7` 已于 2026-08-05 在本地生成正式 Windows 安装包、便携版和更新签名；当前提交尚未推送远端，也未创建 GitHub Release。
- 当前时间源顺序：本机、北京、京东、京东秒送、美团、美团闪购、淘宝、淘宝闪购、大麦、拼多多。
- 当前应用代码和 `0.1.7` 正式包已通过本地验证；后续功能以用户新请求为准。
