{
  "name": "floating-clock",
  "version": "0.1.0",
  "description": "A lightweight Windows floating clock with marketplace time-source switching and countdown mode.",
  "scripts": {
    "dev": "tauri dev",
    "tauri:build": "tauri build",
    "test": "node --test"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
