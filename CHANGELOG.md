# 更新日志

本文件记录用户可见的功能、行为和兼容性变化。

## 开发沿革

- **2026-05-21：原型开始。** 最早的项目会话确定了 Windows 悬浮时钟的核心目标：实时时钟与倒计时切换、北京时间/京东/拼多多/淘宝时间源、固定尺寸无边框窗口、拖动、托盘、全局快捷键、置顶、倒计时归零和临界五秒高亮。该阶段早于当前仓库的可复现 Git 历史，因此仅作为开发沿革记录，不视为已发布版本。
- **2026-07-19：跨虚拟桌面可见性诊断。** 确认“窗口置顶”只控制当前 Windows 虚拟桌面的层级；若需跨桌面显示，须在 Windows 任务视图中单独设置。此次为只读诊断，没有功能改动。
- **2026-07-22：当前仓库历史开始。** 首个可复现提交为 `d436212`（`feat: 初始化悬浮时钟项目`）。仓库未包含 Git tag 或发布构件记录；下列 `0.1.0` 与 `0.1.1` 区间按历史版本清单及对应提交追溯。

## [0.1.3] - 2026-07-27

### 新增

- 京东、美团、美团闪购、淘宝闪购和大麦新增平台毫秒级时间策略，至此全部 8 个时间源默认可显示毫秒：京东取 `api.m.jd.com` 响应头 `X-API-Request-Id` 的毫秒段并与 `Date` 头交叉校验；美团与美团闪购使用 `cube.meituan.com` 服务器时间接口（共用美团时基，独立采样）；淘宝闪购与大麦分别使用饿了么、大麦自己的 MTop 网关 `mtop.common.getTimestamp`。全部端点均为匿名只读请求，无 Cookie、无签名。

### 改进

- 原 HTTP `Date` 秒边界校准降级为毫秒策略失败时的自动回退，行为与红字提示保持不变；对应调研与实测证据见 `docs/时间源毫秒端点实测调研.md`。

## [0.1.2] - 2026-07-26

### 新增

- 新增独立于“时钟 / 倒计时”内容模式的 Mini 界面模式。Mini 显示当前时间源、当前时间或倒计时数值及日期，并复用既有校时、倒计时、归零和最后五秒高亮规则。
- Mini 支持从标准标题栏左侧图标、标题栏可拖动空白区域双击和托盘菜单双向切换；Mini 的时间区域双击可恢复标准模式。
- Mini 右侧提供与标准窗口一致语义的三个紧凑图标操作：退出应用、恢复标准模式和隐藏到托盘。
- 新增大麦时间源。
- 标准模式主时间数字可在“显示毫秒”和“显示整秒”之间点击切换；Mini 继承当前精度，但不增加点击切换交互。
- 标准标题栏和 Mini 时间区域增加“**双击切换显示**”提示。

### 改进

- Mini 在本次运行内固定置顶；进入 Mini 时锁定托盘中的“窗口置顶”，退出后恢复标准模式原有的置顶偏好。
- Mini 使用 Tauri 原生命令统一管理展示模式、窗口尺寸和置顶状态；标题栏与托盘通过 `window-presentation-changed` 事件保持同步。
- Mini 宽度按数值实际长度调整并设置最小宽度；超长倒计时仅在位数变化时重新调整，避免每帧调整窗口大小。
- Mini 时间数字使用等宽数字，保留全窗口拖动区域；右侧操作按钮及其周边不参与拖动。
- 标准标题栏改为紧凑图标入口并保持标题居中；移除旧的拖动提示与点阵握把。
- 日期显示在日期与星期之间增加空格，标准模式和 Mini 保持一致。
- 使用 Tauri 的 `data-tauri-drag-region` 统一拖动区域，消除部分按钮在鼠标悬停时指针与手形光标来回切换的问题。
- 时间显示会按实际校准能力自动选择默认精度：严格毫秒校准默认显示毫秒；HTTP `Date` 或本次毫秒校准质量不足的来源默认显示整秒。完成校准后，用户仍可手动查看推算毫秒，状态栏会明确提示“非平台级严格毫秒”或“毫秒校准失败”。
- 北京时间增加多 NTP 服务回退；应用启动后每 10 分钟自动重新校准。
- 倒计时始终使用当前选中的时间源，不再在切换倒计时时自动改用北京时间。
- 优化 Mini 模式的三行文字对齐，以及标准和 Mini 模式下提示的位置。

### 修复

- 修正 HTTP `Date` 时间源因无法满足严格毫秒门槛而频繁校准失败的问题：取得有效秒级校准后可正常使用，并明确标示其精度边界。
- 校准失败时，状态栏明确显示沿用上次校准或暂用本机时间的当前回退状态。

### 兼容性与行为

- Mini 状态不跨重启保存；应用下次启动仍进入标准界面，既有时钟/倒计时选择、时间源与窗口位置规则保持不变。
- Mini 的固定置顶只作用于当前 Windows 虚拟桌面；需要跨虚拟桌面显示时，仍需在 Windows 任务视图中设置。
- 鼠标穿透开启时，Mini 无法悬停或拖动；可继续通过托盘关闭鼠标穿透、恢复标准模式或显示窗口。

### 测试

- 增加 Mini 展示模式、原生拖动、标题栏/双击切换、Mini 操作按钮和窗口桥接事件的前端与桥接层回归测试。
- 增加标准时间数字精度切换、页面内 Tooltip 和 Mini 继承精度状态的回归测试。
- Rust 测试覆盖 Mini 强制置顶、拒绝 Mini 中取消置顶和 Mini 宽度边界。
- 补充大麦时间源、多 NTP 回退、10 分钟自动校准、精度降级与切换、倒计时保持当前来源及 Mini 布局的回归测试。

## [0.1.1] - 2026-07-22

本版本将 `package.json`、`src-tauri/Cargo.toml` 与 `src-tauri/tauri.conf.json` 的版本号同步升级为 `0.1.1`。

### 新增

- 北京时间改用国家授时中心 `ntp.ntsc.ac.cn:123` 的 NTP 时间戳，提供毫秒级校时。
- 时间源扩展为北京时间、京东、拼多多、淘宝、美团、美团闪购和淘宝闪购共七项。
- 为没有公开毫秒时间字段的平台增加 HTTP `Date` 秒边界相位校准：使用独立的无缓存连续请求估计毫秒偏移及误差窗口，不将秒级响应伪装为毫秒时间。
- 新增 `build.ps1` 自动打包脚本：调用 Tauri 构建后，将 NSIS 安装包和便携版复制到 `dist/`，并以版本号命名。
- 新增《打包流程》文档，明确正式打包入口、版本同步要求、构建产物边界和发布验收文件名。

### 改进

- 直接毫秒接口与 NTP 校时均最多采样三次，并按最短 RTT 选择样本；相位校准展示校准窗口与误差估计。
- 拼多多和淘宝在直接毫秒接口失败后，改为独立的相位校准；美团与闪购时间源彼此独立采样，不共享偏移。
- Tauri 的 Rust 网络层仅接受预定义时间策略，避免前端传入任意请求地址。

### 测试

- 补充 NTP 时间戳解析、NTP 本地计时样本、秒边界相位校准及全部七个时间源的毫秒校时测试。
- 补充 Tauri 桥接层的 NTP、美团相位校准和完整时间源覆盖测试。

## [0.1.0] - 2026-07-22

此区间覆盖首个可复现提交 `d436212` 至版本号升级为 `0.1.1` 前的全部 `0.1.0` 提交；期间经历了从 Electron 原型到 Tauri 单版本的迁移。

### 新增

- 建立 Windows 悬浮时钟基础能力：无边框固定尺寸窗口、标题栏拖动、任务栏与托盘召回、单实例、`Ctrl+Alt+T` 显示/隐藏、窗口位置记忆、开机启动、鼠标穿透和窗口置顶。
- 提供实时时钟与北京时间倒计时两种模式，支持下一整点、下一个 `10:00`、下一个 `20:00` 快捷目标，以及整点或目标时刻前最后五秒高亮。
- 提供北京时间、京东、拼多多和淘宝时间源；每种策略最多采样三次，按 RTT 最短样本计算本机偏移，并在校时失败时保留已成功的当前源偏移或暂用本机时间。
- 引入 Tauri 轻量 Windows 分发：复用渲染层，由 Rust 发起时间请求，支持 NSIS 安装包与便携版构建。
- 增加时间源菜单、窗口启动行为和 Tauri 桥接层的回归测试。

### 改进

- 完成本地化 README，并补充时间源精度、回退策略、托盘行为和构建方式说明。
- 标题栏调整为可拖动区域与居中的应用标题；时间源选择由原生下拉框改为带当前选择状态的应用内菜单。
- 将窗口高度统一为 `392`，使时钟和倒计时内容具有稳定布局。
- 从同时维护 Electron 和 Tauri 的双版本，收敛为 Tauri + WebView2 单版本；移除 Electron 主进程、预加载脚本和旧托盘图标资产。

### 修复

- 窗口状态插件仅恢复位置、不再恢复错误的尺寸，避免窗口高度在重启后异常。
- 应用启动时在窗口准备完成后显示并获得焦点；最小化或按 `Esc` 隐藏到托盘，关闭按钮、`Alt+F4` 和托盘“退出”则真正退出应用。
